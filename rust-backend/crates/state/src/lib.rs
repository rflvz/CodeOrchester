// State crate stub — full implementation in DAW-545.
// Provides shared state types for the CodeOrchester Rust backend.

/// Placeholder for the centralised agent/team state manager.
/// Will be expanded in DAW-545.
pub struct StateManager;

impl StateManager {
    pub fn new() -> Self {
        StateManager
    }
}

impl Default for StateManager {
    fn default() -> Self {
        Self::new()
    }
}
